# local runtime state
*.log
.graft/
knowledge-base/
.inventory.tsv
telemetry.tsv

# node
node_modules/
*.tsbuildinfo

# subagent artifacts
.pi-subagents/

# macOS
.DS_Store
__pycache__/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# graft runtime DB + build artifacts
vendor/graft/graft.db*
vendor/graft/build/
vendor/graft/models/
vendor/graft/third_party/

# rust build artifacts
target/

# exported ONNX model — regenerate with rs/heimdall-embed/export_onnx.py
rs/heimdall-embed/model/
