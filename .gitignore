# local runtime state
*.log
.graft/
knowledge-base/
.inventory.tsv
telemetry.tsv

# node
node_modules/
*.tsbuildinfo
*.tgz  # npm pack output

# subagent artifacts
.pi-subagents/

# macOS
.DS_Store
__pycache__/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# graft runtime DB + build artifacts
vendor/graft/graft.db*
vendor/graft/build/
vendor/graft/models/
vendor/graft/third_party/

# rust build artifacts (from any vendored/past cargo workspace)
target/

# exported ONNX model — local 127 MB blob, regenerable, never committed
rs/**/model/
*.tgz
